# CLI Commands

ArtReactor provides the `arte` CLI for managing the service and plugins.

## Available Commands

- `arte start` - Start the service
- `arte plugin create` - Create a new plugin
- `arte plugin install` - Install a plugin
- `arte plugin templates` - List plugin templates

Run `arte --help` for complete documentation.
